name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# 동시 배포 1건만 유지 (진행 중 실행은 취소하지 않고 큐잉)
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Build static site
        run: node scripts/build.mjs
        env:
          # 커스텀 루트 도메인(health-stories.org) 배포 — 하위 경로 없음
          BASE_PATH: ""
          SITE_URL: https://health-stories.org
      - name: Configure Pages
        # enablement: true → 워크플로에서 Pages 사이트 자동 활성화 시도.
        # (저장소 Settings → Pages → Source: GitHub Actions 가 이미 켜져 있으면 그대로 사용)
        uses: actions/configure-pages@v5
        with:
          enablement: true
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
